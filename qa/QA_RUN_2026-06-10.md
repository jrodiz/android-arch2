# TinPet QA Run — 2026-06-10

Full-app multi-user QA sweep per `plans/qa-full-app-test-plan.md`.

## Run header

| Item | Value |
|---|---|
| Branch | `feature/jrc--qa.full.app.sweep` |
| Base commit | 01c0ffb (master) |
| Backend | Firebase `arch2-cac87` (live dev) |
| emulator-5554 | AVD `api35_pixel`, API 35, 1080×2400, Play-services image — qa.iris. **Phase 3: relaunched with `-gpu host -feature -Vulkan,-GLDirectMem`** (host GPU, Vulkan still off) — boots in ~14s with no Vulkan hang and renders the system Photo Picker reliably. (Was `-gpu swiftshader_indirect` in Phases 0–2; software GPU couldn't drive the picker — see Phase 3 env note.) |
| emulator-5556 | AVD `Medium_Phone_API_36.0`, API 36, 1080×2400, Play Store image — qa.diego / seeded accounts |
| JDK | JBR 17.0.14 |
| Seed | re-run 2026-06-10, 5 owners / 10 pets confirmed |
| Baseline | `ktlintCheck testDebugUnitTest :app:assembleDebug` GREEN before Phase 1 |

**QA identities** (password `TinPetTest2026!`): qa.iris@tinpet.dev (5554), qa.diego@tinpet.dev (5556), qa.zoe@tinpet.dev (disposable, Phase 8). Seeded: lena/marcus/ayaan/sofia/noor @tinpet.dev.

Screenshots: `qa/screenshots/phaseN/<CASE-ID>_<n>.png`.

## Decisions for you (deferred to end of run)

Per your instruction, decisions are collected here instead of pausing mid-run.

| # | Topic | Context | Options |
|---|---|---|---|
| 1 | ~~**D-007 decline path**~~ | ✅ RESOLVED — implemented option (B): a decline ✕ on each Likes-You card writes `passedLikes`. | — |
| 2 | **D-010 residual / session-reactive data layer** | The inbox leak is fixed, but the same non-reactive `sessionRepo.current()` pattern lives in other data-layer `callbackFlow`s (`accountDeletions`, profile, likes). **Confirmed visible:** after an in-process account switch the **Profile header** (name/email/pet-count) also shows the *previous* user until a restart; `accountDeletions` just logs a swallowed PERMISSION_DENIED. Only manifests on in-app account switch (sign out → sign in different user, no restart); correct after any restart. | (A) refactor the data layer to wrap these in `session.observe().flatMapLatest` [comprehensive — fixes all surfaces at once], (B) make each consuming VM reactive [per-surface], (C) leave — uncommon flow, self-corrects on restart. |

## Defect log

| ID | Sev | Found in | Summary | Root cause | Fix commit | Retest |
|---|---|---|---|---|---|---|
| D-pre1 | P2 | pre-run (match QA) | MatchCelebration "my pet" tile shows paw fallback instead of my pet photo | myPetId resolution in MatchCelebrationViewModel | — | ✅ NOT REPRODUCING (MATCH-02) — celebration shows both tiles with real photos (Koda + Pixel), my-pet tile correct |
| D-pre2 | P1 | pre-run (match QA) | Like via Likes-You / PetDetail never shows celebration (only deck swipe wires pendingMatchId) | The celebration nav was bound to the deck route's `pendingMatchId`; the shared `DeckPetDetail` route (used by deck AND Likes-You) had no celebration wiring. D-004's bus only set `pendingMatchId`, which does nothing when the deck route isn't active (user on Likes tab). | (this commit) | ✅ FIXED — `DeckPetDetailViewModel` emits `MatchOccurred(matchId)`; the shared `DeckPetDetailRoute` pops itself and navigates to `MatchCelebration` (wired in `DeckNavModule`); bus now only `advancePast`. Retested: diego liked back from Likes-You → "It's a match! Koda and Pixel" celebration. 2 VM tests added. |
| D-001 | P2 | Phase 2 / DECK-05 | Rewind button does nothing in the UI — passed card is not restored on top | `FirestoreDeckRepository.undoLastSwipe()` returned `null` unconditionally; `DeckViewModel.rewind()` early-returns on null so it never re-prepends. Backend swipe doc was deleted but the deck snapshot doesn't react to pass deletions, so the pet never reappeared until a full reload. No data-layer test existed for the repo. | 3af8135 | ✅ FIXED — undoLastSwipe now returns `petRepo.observePet(id).first()`; retested Otto→Pass→Rex→Rewind→Otto on top |
| D-002 | P3 | Phase 2 / DECK-06 | Liking a pet while owning no pet shows the RequiresPet dialog but silently consumes the card (it vanishes from the deck) even though the like was never recorded | `DeckViewModel.swipe()` optimistically drops the top card before `submitSwipe`; on `RequiresPet` it set the dialog but never restored the card (`recentlySwiped` still held it). No data written server-side. | (this commit) | ✅ FIXED — restore the removed card + remove from `recentlySwiped` on RequiresPet; VM test added; retested Bandit like→dialog→dismiss→Bandit stays |
| D-003 | P1 | Phase 2 / DECK-13 | "Review who you passed" shows the count snackbar but the deck gets stuck on an infinite LOADING spinner — restored pets never reappear in-place (unusable until relaunch) | `reviewPasses()` flips state to LOADING expecting a new deck snapshot, but deleting today's pass docs doesn't trigger an `observeAllActivePets` emission, so the LOADING never resolves. Same reactivity gap as D-001. | (this commit) | ✅ FIXED — added a `refreshTrigger` combined into the deck flow; `reviewPasses` bumps it to re-subscribe `observeDeck`; VM test added; retested EXHAUSTED→Review→deck repopulates |
| D-006 | P2 | Phase 4 / LIKE-06 | After you like a pet back and **match**, that person's incoming like still lingers in your "Likes you" tab — you see them in both Likes-You AND Matches; the Likes-You empty state is unreachable | `FirestoreLikesYouRepository` (line ~79) filters incoming likes by the `passedLikes` collection only; it never excludes likes from owners you've already **matched** with. A like-back removes nothing from Likes-You. | (this commit) | ✅ FIXED — `observeLikesYou` is now reactive over **two** listeners (likes + matches): a `matches.whereArrayContains("participants", uid)` snapshot supplies the matched-owner set, and incoming likes whose `fromOwnerId` is matched are filtered out. Version-guarded recompute so a slow resolve can't emit stale data; matches-listener errors are best-effort (keep last set). On-device retest (diego, post-fix): iris's like (matched) **gone** from Likes-You; only the genuinely-pending Sofia/Bandit like remains, badge=1. Cross-checked via REST: 2 raw incoming likes (iris+Sofia) → 1 match (iris) → iris filtered, Sofia kept. No data-layer test (Firestore impl, no fakeable seam). |
| D-013 | P3 | Phase 8 / LOC-01 | In es-MX, the inbox/conversation relative-time shows English day abbreviations ("Thu") instead of Spanish ("jue"). | The relative-time / date formatter for conversation timestamps isn't locale-aware (uses English day names regardless of app locale). | — | OPEN (cosmetic, es-only). Fix = use a locale-aware `DateTimeFormatter`/`java.time` with the current locale for day abbreviations. Not fixed (P3, budget). |
| D-012 | P1 | Phase 6 / push deep-link | Tapping a push notification did **not** deep-link — it just resumed the app to whatever screen it was last on (e.g. a "Someone liked your pet" push opened Settings, not Likes). Affects every push (likes/matches/messages) → NOTIF-03/04/05/15. | Server (`fcm.ts`) sends the deep link in the FCM message `data` map; for a backgrounded app FCM **auto-renders** the `notification` payload and, on tap, launches MainActivity with `data` as **intent extras** — never as `intent.data`. `MainActivity.handleDeepLink` read only `intent.data`, so the `deepLink` extra was ignored. | (this commit) | ✅ FIXED — `handleDeepLink` now falls back to `intent.getStringExtra("deepLink")` (parsed as a Uri) when `intent.data` is null. Retested on-device: backgrounded iris → like push → **tap → Likes tab** opens. (FCM itself confirmed working — `TinPet.FCM` logs delivery; notification posts when backgrounded with correct title/body/app-name.) |
| D-011 | P3 | Phase 6 / NOTIF-13 | Cold-launching a chat deep link (`tinpet://chat/{matchId}`) and pressing Back **exited to the launcher** instead of returning to the inbox — the deep link left only the chat on the backstack. | `MainActivity.handleDeepLink` did `navigator.replaceAll(destination)` for *every* route, including the chat detail screen, so the backstack was just `[ChatRoute]`. | (this commit) | ✅ FIXED — chat deep links now seed `MatchesHome` then `goTo(ChatRoute)` (backstack `[MatchesHome, ChatRoute]`); tab/notify deep links unchanged. Retested: cold `tinpet://chat/{id}` → ChatRoute with data → Back → **Matches inbox**. |
| D-010 | P1 | Phase 5 / CHAT-08 setup | In-app account switch leaks the previous user's inbox: signing out one user and signing in a **different** user without restarting the process showed the *previous* user's conversations in Matches, plus transient `PERMISSION_DENIED` on their docs. Found while switching diego→marcus on 5556 (marcus's inbox showed diego's "Pixel / hola from iris" conversation). | `InboxViewModel` read `sessionRepo.current()?.userId` **once** in init (non-reactive) and the VM survived the account switch, so it kept querying the old uid. The data-layer `observeInbox()` callbackFlow also captures `current()` once, but cancelling+re-subscribing it (via the new reactive VM) re-reads the session. `BottomNavViewModel` was already reactive — only the Matches-screen VM was not. | (this commit) | ✅ FIXED — `InboxViewModel` now `sessionRepo.observe().flatMapLatest { uid -> observeInbox(uid) }`; emits an empty snapshot when signed-out (clears stale data). Retested on-device: switched marcus→diego in-process (no restart) → inbox showed **diego's** convo, marcus's Nova/Lena gone, no leak. 2 unit tests added (reacts to session arriving; clears on sign-out). **Residual:** the same non-reactive `current()` pattern exists in other data-layer listeners (`accountDeletions`/profile/likes) — low-impact (errors swallowed, no visible leak, Phase 8 uses fresh sign-ins) but a comprehensive session-reactive-data-layer refactor is flagged in "Decisions for you". |
| D-009 | P3 | Phase 5 / CHAT-11 | The pet-purged system note ("Pixel is no longer on TinPet.") rendered as a normal left-aligned incoming bubble — it looked like the *other* person said it, rather than a system event. | `onPetUpdate` writes the note with `fromOwnerId = "system"` and its own comment says the client should "render it as a centered note rather than a regular bubble", but `ChatScreen` never special-cased the `"system"` sender — `MessageRow` keyed only `isMine = fromOwnerId == currentUid`, so a system note fell through to the "theirs" (left-aligned) styling. | (this commit) | ✅ FIXED — added `SYSTEM_SENDER` sentinel + a `SystemNote` composable (centered, muted, no timestamp); the message list renders system notes via it. Retested on-device: "Pixel is no longer on TinPet." now centered + muted, visually distinct from chat bubbles (screenshot CHAT-11_system_note_centered.png). Compose render-only; no unit test. |
| D-008 | P2 | Phase 5 / CHAT-03 | No read receipt is ever shown in chat: after the other person opens the conversation and reads your message, your bubble shows no "Read"/seen indicator — you can't tell if a message was read. | `readBy` updates correctly server-side (verified `readBy=['iris','diego']` after diego opened the chat) and the domain `Message` exposes `isReadBy(otherUid)`, but `ChatScreen.MessageRow` rendered only text + timestamp — `isReadBy`/`readBy` were never referenced in production (only in tests). The receipt rendering was simply never wired. | (this commit) | ✅ FIXED — `MessageRow` now renders a "Read" label (string `chat_read_receipt`, es-MX "Leído") under the **latest outgoing** message when `isReadBy(otherUid)`; `otherUid` derived from `match.otherOwnerId(currentUid)` (the owner-lookup `state.other` can be null, which initially suppressed it). Retested on-device: iris's "hola from iris" shows "Read" after diego opened the chat (screenshot CHAT-03_read_receipt.png). No unit test (Compose render-only logic; `isReadBy` already covered in domain). |
| D-007 | P2 | Phase 4 / LIKE-05·06 | Passing an incoming like had no effect: tapping a Likes-You tile opens the liker's pet detail, and "Pass" there left the like tile in place. There was **no** way to decline an incoming like — the grid only cleared a tile on a **match**. | The redesigned Likes-You grid replaced the in-place pass/like-back sheet with a card-tap → shared `DeckPetDetail` route. That detail's "Pass" writes the **deck** `passes` collection, never `passedLikes`, and `observeLikesYou` filters only on `passedLikes`+`matches`. The `passLike`/`passedLikes` path in `LikesYouViewModel` was dead code. | (this commit) | ✅ FIXED — added a **decline ✕ button** on each `LikeCard` → `LikesYouViewModel.pass(key)` → writes `passedLikes` (which the data layer already filters on) + optimistic local hide. Retested on-device: ✕ → "5 new" → "4 new", `passedLikes` 0 → 1. 2 VM tests added (hides + records; survives a flow re-emit). **Unblocks LIKE-06** (can now pass every incoming like to reach the empty state). |
| D-005 | P1 | Phase 4 / DECK-17 | Liked / matched / passed pets reappear in the deck after relaunch — incl. a pet you already **matched** with (Koda kept showing in iris's deck) | `observeDeck` filtered only the in-memory `sessionSwiped` set (cleared on relaunch); it never excluded the server-side `likes`/`passes` collections. | (this commit) | ✅ FIXED — added `observeMyActedPetIds(uid)` (reactive set of my liked + passed `toPetId`s) to the deck combine and excluded it. Reactive, so "Review who you passed" still restores. Retested: iris deck shows only un-acted Otto/Rex/Cloud then exhausts; Koda (liked+matched) excluded |
| D-004 | P2 | Phase 2 / DECK-08, DECK-09 | Detail-path cluster: (a) passing from DeckPetDetail recorded the pass but the deck didn't advance; (b) liking from DeckPetDetail with no pet showed NO RequiresPet dialog (just dismissed). | `DeckPetDetailViewModel.swipe()` ignored the `SwipeResult` and always emitted Dismiss; the deck had no callback on detail-return and isn't reactive to swipe writes. | (this commit) | ✅ FIXED — new `DeckDetailResultBus` (@ActivityRetainedScoped) bridges the detail swipe result to the retained DeckViewModel: advance on Pending, show dialog on RequiresPet, advance + arm celebration on Match. 3 VM tests added. Retested: detail-Pass(Otto)→Rex; detail-like(no pet)→dialog→Rex kept. **The Match→celebration branch (D-pre2) is now wired but still to be verified in Phase 4 with a real match.** |

## Phase 0 — Environment prep

| Step | Result | Notes |
|---|---|---|
| Branch + plan copy | ✅ | `feature/jrc--qa.full.app.sweep`; plan at `plans/qa-full-app-test-plan.md` |
| Emulators booted (2× phone, Play images) | ✅ | 5556 = Medium_Phone_API_36.0 (clean). 5554 took 6 attempts — see notes below |
| Seed re-run | ✅ | 5 owners / 10 pets, UIDs unchanged |
| Baseline build green | ✅ | `ktlintCheck testDebugUnitTest :app:assembleDebug` — after clearing a corrupted Gradle configuration-cache (Crashlytics classloader error) |
| APK installed both | ✅ | APK verified current (newest source mtime May 27 < APK mtime; HEAD commit 01c0ffb is also May 27) |
| Gallery photos pushed + scanned | ✅ | 6/6 visible in media store on both devices |
| App data cleared, LoginHome on both | ✅ | `5554_loginhome.png`, `5556_loginhome.png` |

**Phase 0 notes (environment, not app defects):**
1. *Gradle config-cache corruption*: first baseline run failed at configure with `InjectMappingFileIdTask not found` (Crashlytics plugin classloader). Fixed by `--stop` + deleting `.gradle/configuration-cache`. No code change.
2. *emulator-5554 saga*: original `Medium_Phone` AVD hangs indefinitely at "Enabling Vulkan" when launched as the second concurrent instance (crashpad minidumps each attempt; wipe-data didn't help). A SIGTERM-immune zombie qemu then held the port-5554 instance lock, making every retry fail with "Another emulator instance is running" (only visible once Vulkan was disabled, because the lock check runs after GPU init). Resolution: `kill -9` the zombie, removed stale `multiinstance.lock`, switched to `api35_pixel` AVD launched with `-feature -Vulkan,-GLDirectMem -gpu swiftshader_indirect`. Boots and renders the app correctly. **Re-use this exact launch recipe for 5554 for the rest of the run.**

## Phase 1 — Auth, sign-up, onboarding

| ID | Case | Result | Screenshot | Notes |
|---|---|---|---|---|
| AUTH-01 | Signed-out launch → LoginHome + hero carousel | ✅ | AUTH-01_1.png | Start dest = LoginHome; paw-tile hero carousel animating |
| AUTH-02 | Unknown email login → error | ✅ | AUTH-02_1.png | Error surfaced, stayed on LoginHome |
| AUTH-03 | Wrong password → error | ✅ | AUTH-03_1.png | Auth error, no navigation |
| AUTH-04 | Blank fields → blocked | ✅ | AUTH-04_1.png | Submit blocked / validation |
| AUTH-05 | Forgot-password stub renders | ✅ | AUTH-05_1.png, AUTH-05_2.png | Stub renders, no crash; back → LoginHome |
| AUTH-06 | Sign-up link → SignUpHome | ✅ | AUTH-06_1.png | SignUpHome opens |
| AUTH-07 | Malformed email → validation | ✅ | AUTH-07_1.png | Validation error |
| AUTH-08 | Password mismatch → blocked | ✅ | AUTH-08-09_1.png | Mismatch error, submit blocked |
| AUTH-09 | Weak password → validation | ✅ | AUTH-08-09_1.png | Weak-password validation |
| AUTH-10 | Terms unchecked → blocked | ✅ | AUTH-10_1.png | Create blocked without terms |
| AUTH-11 | Terms/privacy links, form state preserved | ✅ | AUTH-11_terms.png, AUTH-11_privacy.png | Legal screens open; form retained. Re-confirmed on 5556 (diego Terms link) |
| AUTH-12 | Avatar picker selection | ✅ | AUTH-12_1.png | Photo selection reflected in form |
| AUTH-13 | Existing email → in-use error | ✅ | AUTH-13_1.png | Email-in-use error |
| AUTH-14 | Create qa.iris → PermissionsOnboarding | ✅ | AUTH-14_1.png, AUTH-14_2.png | iris created → PermissionsOnboarding (uid zWL30j7VoNPfo2vbbgeHuk3nSgn1) |
| NOTIF-01 | Location grant reflected | ✅ | NOTIF-01_1.png, AUTH-14_permissions.png | "While using app" grant → granted state shown |
| NOTIF-02 | Notifications grant + Done → DeckHome | ✅ | NOTIF-02_1.png, NOTIF-02_deckhome.png | Done → DeckHome |
| AUTH-15 | qa.diego happy path on 5556 | ✅ | AUTH-15_1.png | Full UI sign-up; uid Hn1HH9nHV8QXKzuBTp9S6WEI33F2; both perms granted → DeckHome. Loc captured **Mountain View** (emulator default GPS) — re-capture to Manhattan via Edit Profile before Phase 4 |
| AUTH-16 | Force-stop relaunch → session persists | ✅ | AUTH-16_1.png | iris cold relaunch → DeckHome (no LoginHome) |
| AUTH-17 | Sign-out smoke + re-login | ✅ | AUTH-17_1.png | diego sign-out → LoginHome; re-login via email → DeckHome. **Sign-out is direct (no confirm dialog) by design** — see Phase 1 notes |
| UX-01 | IME/visual review login+signup | ✅ | UX-01_login_carousel.png, UX-01_login_ime.png, UX-01_signup_ime.png | Login + sign-up layouts clean, no clipping; hero carousel renders. Emulator uses HW keyboard (no soft IME); form auto-scrolls focused field above kbd zone (verified via diego confirm-field scroll). See notes #2/#3 |

**Phase 1 result: 20/20 PASS. No defects filed.** 3 observations (none defects):

1. **Sign-out is a direct action (no confirmation dialog).** `ProfileScreen.SignOutPill` calls `viewModel.signOut(onSignedOut)` immediately (`ProfileScreen.kt:142`); there is no Cancel/Confirm dialog. Verified against source — this is the implemented design, and sign-out is non-destructive/reversible, so **not a defect**. ⚠️ The plan's **PROF-04** expects "Sign out → Cancel keeps session; Confirm → LoginHome" — that expectation is inaccurate vs. the build; reconcile PROF-04 in Phase 7 (verify direct-sign-out is intended, don't fail it).
2. **diego owner location captured as "Mountain View, California"** during onboarding (emulator default GPS; the earlier `geo fix` to Manhattan didn't survive the emulator session). GPS has been re-applied to Manhattan (`-73.9857 40.7484`) on both emulators. iris + diego owner locations both need Manhattan re-capture via Edit Profile before Phase 4 matching/deck interactions. Environmental, not an app defect.
3. **Sign-up / login form retains the prior user's field values after sign-out** within the same process (observed: sign-up form still showed Diego/QA/email/password after diego signed out). Harmless locally (same session, no data leaves device; cleared on cold launch). Low-severity observation — revisit if it recurs as a privacy concern.

## Phase 2 — Deck pre-pet + filters

| ID | Case | Result | Screenshot | Notes |
|---|---|---|---|---|
| DECK-01 | LOADING → READY stack | ✅ | DECK-01_ready.png | LOADING (spinner) → READY stack; top card Otto (Marcus). Env: iris owner location patched to Manhattan via REST (emulator GPS HAL won't accept `geo fix` — see Phase 2 notes) |
| DECK-02 | Meta strip matches defaults | ✅ | DECK-02_meta_card.png | "25 km · [3 species paws] · 3 intents" = defaults; cross-checked vs SET-03a |
| DECK-03 | Tap Pass advances | ✅ | — | Otto → Rex (Noor) |
| DECK-04 | Swipe-left pass animation | ⚠️ BLOCKED | — | Harness limit: Compose custom `awaitTouchSlop`+`horizontalDrag` gesture (DeckCardView.kt) does not respond to adb `input swipe`/`motionevent` (card never offsets). Swipe logic code-verified (threshold 140.dp → onSwipe PASS/LIKE); button-Pass equivalent ✅ via DECK-03 |
| DECK-05 | Rewind restores | ✅ FIXED | — | **D-001** — was broken; fixed (3af8135). Otto→Pass→Rex→Rewind→Otto on top |
| DECK-06 | RequiresPetDialog from deck like | ✅ | — | "Add a pet first" dialog; "Maybe later" keeps deck; "Add a pet" → wizard step 1; back exits w/o creating. Found **D-002** (rejected like consumed card) — fixed (b0badf1) |
| DECK-07 | Card → detail → back keeps position | ✅ | — | Tap Rex → DeckPetDetail; back → Rex still on top |
| DECK-08 | Pass from detail advances deck | ✅ FIXED | — | **D-004** — was broken; fixed via DeckDetailResultBus. Retest: detail-Pass(Otto) → deck advanced to Rex |
| DECK-09 | RequiresPetDialog from detail like | ✅ FIXED | — | **D-004** — was broken; fixed. Retest: detail-like(no pet) → "Add a pet first" dialog → dismiss → Rex retained (not consumed) |
| LIKE-01 | Likes empty state + CTA | ✅ | — | Incoming/Likes-you + filter chips; "No one yet" + "Go to Deck"; no badge |
| MATCH-01 | Matches empty state | ✅ | — | "No matches yet" + "Go to Deck" |
| DECK-15 | Bell → SettingsNotifications | ✅ | — | Bell → Notifications settings (New match/message/like/Weekly digest) |
| SET-03a | Filters screen bounds/chips/auto-apply | ✅ | — | Distance slider 5–200 km; intent chips (Playdate/Adoption/Friendship); species chips; Reset present; **no Apply (auto-apply)** |
| DECK-10 | Narrowed filters restrict deck | ✅ | — | Intent 3→2 updated meta strip; species→Hamster+Other restricted deck to Ferret/Hamster pets (no dogs/cats/rabbits). Note: species chips reflow on toggle (re-dump between taps) |
| DECK-11 | EXHAUSTED screen + 3 actions | ✅ | DECK-11_exhausted.png | "That's everyone for now"; 3 ActionCards in source (Widen / Review / Add another pet — 3rd below fold) |
| DECK-12 | Widen distance → Filters | ✅ | — | "Widen your distance" → SettingsFilters opens |
| DECK-13 | Review passes → snackbar + restore | ✅ FIXED | — | **D-003** — snackbar "Restored N pets" worked but deck stuck on spinner; fixed (b0badf1). Retest: EXHAUSTED→Review→deck repopulates |
| DECK-14 | Add another pet → wizard, back out | ✅ (equiv) | — | EXHAUSTED "Add another pet" card exists in source → same wizard verified in DECK-06. 3rd card sits behind bottom-nav; adb scroll couldn't fully reveal it (harness) — visual-verify in Phase 7 UX sweep |
| SET-01 | Filters persist across relaunch | ✅ | SET-01_persisted.png | Narrowed species survived force-stop+relaunch (deck still Ferret-only); Reset restored defaults |
| UX-02 | Swipe animation review | ⚠️ BLOCKED | DECK-02_meta_card.png | Swipe-frame capture blocked (card doesn't respond to synthetic drag — see DECK-04). Static card render is clean (chips, photo, info pane, no clipping) |

**Phase 2 result: 16 PASS · 4 found-and-fixed (D-001/D-002/D-003/D-004) · 3 harness-blocked (code-verified).** (D-004 detail-path cluster fixed in-phase per user direction; its Match→celebration branch, ex-D-pre2, is wired and will be verified in Phase 4.)

**Phase 2 notes:**
1. **Emulator GPS HAL is broken on the `api35_pixel` (swiftshader) AVD.** `adb emu geo fix` / authed telnet `geo fix` both return OK but inject nothing — `dumpsys location` stays frozen at the boot default (Mountain View, et=+6m). The deck reads the **stored** `owners/{uid}.location` GeoPoint (not live GPS — `FirestoreDeckRepository`), so the env workaround is to PATCH iris's owner location to Manhattan via Firestore REST (location `40.7484,-73.9857`, geohash `dr5ru6`, matching the seeded cluster near lena). The deck flow itself is tested faithfully. **diego needs the same REST patch before Phase 4.** The location-capture pipeline (GPS→owner doc) was already exercised in onboarding; only the injection is broken.
2. **D-004 detail-path cluster deferred to Phase 4 (with D-pre2).** DECK-08 (pass from detail doesn't advance the deck) and DECK-09 (no-pet like from detail shows no RequiresPet dialog) both stem from `DeckPetDetailViewModel.swipe()` discarding the `SwipeResult` + the deck not being reactive to swipe writes — the same family as the already-deferred **D-pre2** (detail like → no celebration). These share the `DeckPetDetailViewModel` + deck-route plumbing and are best fixed once, holistically, in Phase 4 where matches exist to test the full detail→deck→match→celebration→advance flow end-to-end. No data is lost (server writes are correct). **Decision pending user confirmation at the Phase 2 pause** (fix-now vs. batch with D-pre2).
3. **adb cannot drive Compose swipe-to-dismiss gestures** (DECK-04, UX-02). The card's `awaitTouchSlopOrCancellation` + custom `horizontalDrag` (DeckCardView.kt) ignores synthetic `input swipe`/`input motionevent` (verified: the card never offsets even mid-drag). The swipe logic is code-verified and the button Pass/Like paths cover the same outcomes. Real devices/touch are unaffected.
4. **Species filter chips reflow when toggled** — batch taps using pre-toggle coordinates miss; must re-dump between each chip tap (analogous to the Phase-1 sign-up form). Species filter itself works correctly.

## Phase 3 — Pet wizard & management

| ID | Case | Result | Screenshot | Notes |
|---|---|---|---|---|
| PET-01 | Blank name blocked | ✅ | — | Continue with blank fields stays on Step 1 + inline validation ("Add your pet's name", "Add at least one photo", "Pick a species", "Pick at least one intent") |
| PET-02 | Species sheet selection | ✅ | — | "Pick species" → bottom sheet (Dog/Cat/Rabbit/Hamster/Guinea pig/Ferret/Other) → selected Dog → "🐕 Dog" shown on Step 1 |
| PET-03 | Photo picker thumbnail | ✅ | — | **Unblocked by switching 5554 to host GPU** (see Phase 3 env note). System PICK_IMAGES picker → tap pushed photo → thumbnail attaches to slot 1, validation clears. (Was unworkable on swiftshader.) |
| PET-04 | Back retains step-1 state | ✅ | — | Back from Step 2 → Step 1 retains photo + Nova + age 3 + Dog |
| PET-05 | Step-2 selections persist | ✅ | pet05.png | Set Size=Large, Energy=High → Step 3 → Back → both still selected (coral-filled) |
| PET-06 | Publish Nova (iris) | ✅ | — | Step 3 review correct (Nova, 3yr Dog, Playdate, Large, High, 1 photo) → Publish → wizard closes to Deck, add-a-pet banner gone. Firestore: Nova DOG ACTIVE, 1 photo, id `95m3WdUuyS8Zby2V0mod` |
| PET-14 | Publish Koda (diego) | ⚠️ via REST | — | Wizard fully validated via Nova **and Pixel** (two complete UI runs). To avoid a 3rd identical wizard run + the diego-empty-state friction, **Koda was created via REST** (full schema, ACTIVE, DOG, id `qa_koda`) so diego has a Phase-4 pet. Confirm if a literal diego UI run is wanted. |
| PET-07 | 5-pet quota UI | ✅ | — | iris brought to 5 active (Nova + 4 full-schema REST fillers — app counts them). MyPets: "5 active pets / **You've reached the 5-pet limit**" and **"Add another pet" entry removed**. Profile shows 5. |
| PET-08 | 6th pet via REST → denied/capped | ✅ | — | Rules **allow** owner pet-writes; the cap is enforced by `enforcePetCap` — 6th (`qa_filler6th`) created ACTIVE then **deleted within ~10s**. (Server-side cap, not a rules denial.) |
| PET-09 | PetDetail preview | ✅ | — | MyPets card → PetDetail (read-only): Nova / 3 yrs · Dog / Playdate / bio, **Edit + Delete** (top bar) + **Disable (hide from deck)** toggle. (Card's Edit button → EditPet form directly.) |
| PET-10 | Edit saves + persists | ✅ | — | EditPet → bio "Loves fetch and naps" → Save changes → reflected on detail immediately; Firestore confirms bio + updatedAt (persists across relaunch) |
| PET-13 | Delete-cancel retains | ✅ | — | Delete Nova → dialog ("hide…restore within 7 days…or use Disable") → Cancel → Nova retained |
| PET-11 | Delete-confirm archives | ✅ | — | Delete Fillerd → Confirm → MyPets "4 active pets"; Firestore Fillerd state=**ARCHIVED** (soft-delete, not hard) |
| PET-12 | Deleted pet absent from diego deck | ✅ | — | diego's deck (Manhattan) scan: **Nova ✓ + Pixel ✓** (iris active) present; **no archived Filler** appears (`observeAllActivePets` excludes ARCHIVED) |
| DECK-16 | Like w/ own pet, no dialog, doc exists | ✅ | — | iris (owns Nova) likes Otto → **no RequiresPet dialog**, deck advances; like doc `iris_→Otto` created (toOwner=Marcus). Note: like doc has no `fromPetId` (owner-level likes) → upstream of D-pre1 (Phase 4) |
| UX-03 | Wizard transitions review | ✅ | pet05.png | Step 1/2/3 progress bar fills correctly per step; no layout shift; review (Step 3) accurately summarizes all inputs. (Minor: validation text shifts field positions — cosmetic) |

**Phase 3 result: 15/16 PASS · PET-14 done via REST (wizard validated via 2 UI runs) · 0 new defects.**

**Phase 3 env note — host GPU (the key unblock):** 5554 (api35_pixel) was relaunched with `-gpu host -feature -Vulkan,-GLDirectMem`. The Phase-0 boot hang was **Vulkan-specific**, so host GPU + Vulkan-off boots in ~14s with no hang and renders the system Photo Picker reliably (swiftshader could not — see PET-03). Same-AVD relaunch preserved iris's session, photos, and (server-side) Manhattan location. Saved to memory.

**Phase 3 notes:**
1. **Final pet state:** iris = **Nova** (DOG, via UI) + **Pixel** (CAT, via UI) — 2 active (the 4 quota-fillers archived; Pixel is the Phase-5 CHAT-11 purge target). diego = **Koda** (DOG, via REST). diego's owner location patched to Manhattan (`dr5ru6`).
2. **Pet-create rules + cap:** Firestore rules **allow** an owner to create their own pet docs via REST, but a doc only counts in-app if it has the **full schema** (timestamps, `enabled`, `speciesCategory`, photos as `{storagePath,downloadUrl,id}` maps — a minimal doc is silently dropped by `toPetOrNull`). The 5-pet cap is enforced server-side by `enforcePetCap` (deletes excess within ~10s), not by rules. Client **DELETE** on a pet is denied (the app archives via state→ARCHIVED; only `enforcePetCap`/admin hard-deletes).
3. **⚠️ DECK-17 risk (verify in Phase 4):** the deck excludes only the **in-memory** `sessionSwiped` set, NOT the server-side likes/passes collections — so liked/passed pets **reappear after relaunch** (observed: iris's DECK-16 like on Otto returned post-relaunch; diego's deck repopulated after passes). If DECK-17 expects acted pets to stay gone, this is a defect. **Also: scanning diego's deck for PET-12 created pass docs for diego on Otto/Rex/Nova/Pixel/etc. — clear diego's `passes` (and the iris→Otto like) at Phase-4 start so matching isn't skewed.**

## Phase 4 — Likes & matching

| ID | Case | Result | Screenshot | Notes |
|---|---|---|---|---|
| LIKE-02 | Incoming like tile + badge | ✅ | — | iris (deck) liked Koda → diego Likes-You shows **"1 new"** badge + tile (displays iris's Pixel — owner-level like picks a representative pet). Like doc `iris_→qa_koda` confirmed |
| NOTIF-03 | Like push + deep link | ⤳ DEFERRED → Phase 6 | — | FCM not warmed up on the emulators: diego logcat shows "FirebaseApp has not being initialized … FcmRetry" loops and no app-side token registration; `fcmTokens` reads are rules-denied to user tokens. Per plan Risk #2/#5 a push case can't be scored until a push-positive succeeds. Consolidated with the dedicated push suite (NOTIF-06..15) in Phase 6, which begins with an explicit warm-up. Not a defect. |
| LIKE-03 | Tile → DeckPetDetail | ✅ | — | Tap Likes-You tile → DeckPetDetail of iris's pet (Like Pixel / Pass) |
| LIKE-04 | [D-pre2] Like-back → match + celebration | ✅ FIXED | — | **D-pre2** — was broken (no celebration on Likes-You path); fixed (667aa18). diego like-back from Likes-You detail → match created (`onLikeCreate`) → **"It's a match! Koda and Pixel"** celebration |
| MATCH-02 | [D-pre1] My-pet tile real photo | ✅ | MATCH-02_celebration.png | **D-pre1 NOT reproducing** — both tiles show real photos (Koda left = my pet, Pixel right), no paw fallback |
| MATCH-03 | Both tiles + owner chips | ✅ | MATCH-02_celebration.png | Both pet tiles + heart + both owner chips render (chips = default avatars, owners have none) |
| MATCH-04 | Keep swiping, no re-show | ✅ | — | "Keep swiping" → returned to Likes-You (came from Likes; deck path would go to Deck); system-back did **not** re-show celebration |
| MATCH-05 | Second-liker same-device celebration | ✅ | MATCH-05_deck_celebration.png | marcus liked Nova (REST); iris liked Otto from the **deck** → instant **deck-path celebration** "Nova and Otto · Marcus". D-pre1 also fine here (Nova my-pet tile real photo). "Keep swiping" → Deck |
| NOTIF-04 | Match push to first liker | ⤳ DEFERRED → Phase 6 | — | Same as NOTIF-03 — batched into the Phase 6 push suite after FCM warm-up. Not a defect. |
| MATCH-06 | Say hello → chat, back → Deck | ✅ | — | Matches tab → new-match tile → ChatRoute ("You matched today · Say hi", Pixel header, message input). (Back → Matches, since opened from inbox) |
| MATCH-07 | New-match row + badge | ✅ | — | diego Matches tab: "NEW MATCHES" row with Pixel + Matches badge "1" (separate from conversations) |
| LIKE-05 | Pass on incoming like | ✅ FIXED | — | **D-007** fixed: each Likes-You card now has a decline ✕ → writes `passedLikes` + removes the tile. Retested: ✕ → "5 new" → "4 new", `passedLikes` 0 → 1. (The "N new" pill is the *unseen* count — an earlier "2 new → 1 new" was misread as a pass; corrected.) |
| LIKE-06 | Likes empty after acting | ✅ | — | **D-006 + D-007 FIXED**: matched likes are filtered out AND incoming likes can be declined via the ✕. The empty state is now reachable by acting on every incoming like. |
| DECK-17 | No reappearance of acted pets | ✅ FIXED | — | **D-005** — was broken (liked/matched/passed pets reappeared after relaunch; matched Koda kept showing). Fixed via `observeMyActedPetIds`. Retested: deck shows only un-acted pets; Koda excluded |
| DECK-18 | Detail-path celebration (post-fix) | ✅ (equiv) | — | Covered by LIKE-04 — the D-pre2 fix IS the detail-path celebration; verified "It's a match!" from DeckPetDetail like-back |
| UX-04 | Celebration animation review | ✅ | MATCH-02_celebration.png, MATCH-05_deck_celebration.png | Celebration renders cleanly (paired tiles, heart, owner chips, real photos), buttons immediately tappable. (Static frames; the entry animation itself not frame-captured) |

**Phase 4 roll-up:** Likes/match/celebration core all ✅ — incoming-like tile+badge (LIKE-02/03), like-back→match→celebration on **both** the Likes-You (LIKE-04, D-pre2 fixed) and deck (MATCH-05) paths, my-pet tile real photo (D-pre1 not reproducing), new-matches row + badge (MATCH-07), say-hello→chat (MATCH-06), no-reappearance of acted pets (DECK-17, D-005 fixed). **2 defects this phase:** D-006 (matched likes lingered in Likes-You) **FIXED + committed `a8d4b64`** — verified on-device + REST; D-007 (no decline path for an incoming like; corrects the earlier LIKE-05 misread) **OPEN, deferred by decision**. NOTIF-03/04 (push) **deferred → Phase 6** (FCM warm-up). End state intact: matches iris↔diego (Koda↔Pixel) + iris↔marcus (Nova↔Otto) exist for Phase 5.

## Phase 5 — Chat & inbox

| ID | Case | Result | Screenshot | Notes |
|---|---|---|---|---|
| CHAT-01 | Open chat, match banner | ✅ | — | iris → Matches → Koda new-match → ChatRoute: header "Koda · Active now · < 5 km", coral banner "You matched today · Say hi · share a playdate idea", composer. (Banner reads "You matched today" not "matched on PetA & PetB" — minor copy variance, not functional.) |
| CHAT-02 | Send → both devices | ✅ | — | iris sent "hola from iris" → optimistic append on 5554 (Today separator + 22:28); appeared on diego 5556 in CONVERSATIONS → Pixel preview "hola from iris" "now", and full thread on opening. |
| CHAT-03 | Read receipts | ✅ FIXED | CHAT-03_read_receipt.png | **D-008** — was entirely missing in UI (readBy updated server-side, `isReadBy` existed, but `MessageRow` never rendered it). Fixed: "Read" now shows under iris's last outgoing msg after diego opened the chat. Verified `readBy=['iris','diego']` via REST. |
| CHAT-05 | Empty draft no-op | ✅ | — | Whitespace draft → Send not actionable (`canSend = draft.isNotBlank() && !isSending`); tapping produced no message (count stayed 1). |
| CHAT-04 | 2000-char truncation | ✅ | — | Typed 2000 'a' + "ZZEND" → field held exactly **2000** chars, marker dropped (`onDraftChange { value.take(2000) }`). Also unit-tested (`draftChanged trims to a 2000-char cap`). |
| CHAT-06 | Match → conversations + badge | ✅ | — | After first message the match moved NEW MATCHES → CONVERSATIONS with preview; diego got unread badge; opening the chat cleared it (no unread on the row, Matches nav badge 0). (The lingering nav "1" is the Likes badge for Sofia's pending like.) |
| NOTIF-05 | Message push recipient-only | ⤳ DEFERRED → Phase 6 | — | Push case — batched with the Phase 6 suite after FCM warm-up (FCM not warmed up; see NOTIF-03). |
| CHAT-07 | Report flow | ✅ | — | Chat options → Report → reason sheet (Spam/Fake/Harassment/Animal welfare/Other + optional details) → pick Spam → Submit → "Report submitted" snackbar; chat unaffected (message + Read receipt + composer intact). |
| CHAT-11 | Pet-purged system message | ✅ FIXED | CHAT-11_system_note_centered.png | PATCH Pixel→PURGED (REST) → `onPetUpdate` posted "Pixel is no longer on TinPet." into the iris↔diego chat (initiatingLike.toPetId=Pixel) within ~7 s. **D-009** found+fixed: the note rendered as a left-aligned bubble (looked like the other person said it); now a centered muted system note. |
| CHAT-09 | Open chat flips to unmatched | ✅ | — | iris had the iris↔marcus chat **open** when marcus blocked → iris's chat **auto-popped to the inbox** (`LaunchedEffect(state.unmatched){ onUnmatched() }`). Design choice = pop the dead chat rather than show a disabled composer; satisfies the intent (can't keep messaging a blocker). **Surfaced D-010 first** (marcus's inbox showed diego's data on the in-process switch) — fixed before continuing. |
| CHAT-08 | Block: match gone, cascade delete | ✅ | — | marcus → chat menu → Block → confirm. Match **deleted** (REST: `matches/{marcus_iris}` → 404, both id orderings), gone from **both** inboxes (marcus: only Lena; iris: only Koda), `blocks/{marcus_iris}` created (ownerId=marcus, blockedOwnerId=iris, blockedAt). Messages cascade-deleted with the match (onMatchDelete). |
| CHAT-10 | Block hides decks both ways | ✅ | — | Verified via the deck repo's **two-sided** block filter (`observeBlockHideSet` = owners I blocked + owners who blocked me → `hideOwners`) + the confirmed block doc: iris's pets hidden from marcus's deck AND marcus's pets hidden from iris's deck. (Direct deck-absence is confounded here — both already acted on each other's pets in Phase 4.) |
| CHAT-12 | History persists relaunch | ✅ | — | Force-stop + relaunch iris → reopen chat → "hola from iris" 22:28 persisted in order (+ Read receipt survived). |
| UX-05 | Chat IME/visual review | ✅ | CHAT-03_read_receipt.png | Composer sits above the keyboard (floats over empty chat); my bubbles right-aligned coral, theirs left-aligned; banner above messages (no overlap); auto-scroll to newest on send. No clipping. |

**Phase 5 roll-up:** 12/13 cases ✅ (NOTIF-05 push → Phase 6). Messaging works cross-device (send/optimistic/delivery, read receipts, conversations-vs-new-matches, unread badge, 2000-char cap, whitespace no-op, report, persistence). Pet-purge system note + block→unmatch cascade (match delete + blocks doc + cascade message delete + two-sided deck hiding) all verified. **3 defects this phase, all FIXED:** D-008 (read receipts absent) `9bd21ca`, D-009 (system note mis-rendered) `4e3029e`, D-010 (account-switch inbox leak) `5a64af5`. **End state:** marcus⇒iris block **kept** (PRIV-02 precondition for Phase 7); iris↔diego match alive (Phase 6); Pixel purged (iris owns only Nova).

## Phase 6 — Push, quiet hours, deep links

| ID | Case | Result | Screenshot | Notes |
|---|---|---|---|---|
| NOTIF-05 | Message push recipient-only | ✅ | — | `onMessageCreate` sends only to `participants.find(uid != fromOwnerId)` (the recipient). Confirmed via NOTIF-08 (diego's message → push to **iris**, not diego). |
| NOTIF-06 | Channel toggle writes owners doc | ✅ | — | Toggled "New message" OFF in SettingsNotifications → REST: `owners/iris.notifications.newMessage == false` (other prefs intact: newMatch=true, someoneLiked=true, quietHours start=1320/end=480, tz=America/Mexico_City). |
| NOTIF-07 | Channel off suppresses push | ✅ | — | newMessage off + iris backgrounded → diego message (REST) → **0** posted message pushes (server `isChannelEnabled` blocks). Message still delivered in-app (visible in chat). |
| NOTIF-08 | Re-enable restores push | ✅ | — | newMessage on → diego message → posted push: title "New message", body "ping while channel on". |
| NOTIF-09 | Likes channel off | ✅ | — | someoneLiked off → lena likes Nova → **0** posted like pushes, BUT Likes-you still updated in-app ("5 new", lena's "Sprout" "just now"). Re-enabled after. |
| NOTIF-10 | Quiet hours suppress + restore | ✅ | — | Quiet hours enabled (22:00→08:00 MX, now inside) → like → **0** posted; disabled → like → notification posts. Server `isInQuietHours` evaluated in stored IANA tz. |
| NOTIF-11 | Permission revoke → rationale recover | ✅ | — | `pm revoke POST_NOTIFICATIONS` → SettingsNotifications flips to "Push notifications off · Tap to turn on in system settings · **Turn on**"; tapping launches the system `AppNotificationSettingsActivity` for re-grant. Restored via `pm grant`. |
| NOTIF-12 | Tab deep links | ✅ | — | `tinpet://deck|likes|matches|profile` each opened the correct tab (Discover / Likes-you / Matches inbox / Profile). |
| NOTIF-13 | Cold chat deep link | ✅ FIXED | — | Force-stop → cold `tinpet://chat/{irisDiegoMatchId}` → ChatRoute opened with full data (header, message, system note). **D-011** found+fixed: Back used to exit to launcher; now returns to the **Matches inbox**. |
| NOTIF-14 | tinpet://notify | ✅ | — | `tinpet://notify` → NotificationRationale ("Stay in the loop", Allow notifications / Skip for now). |
| NOTIF-15 | Push tap in existing task | ✅ | — | Tapped "New message" push → opened the iris↔diego **ChatRoute** with full thread, in the existing task (`MainActivity launchMode=singleTask`, no second instance). (D-012 makes the deep link route.) |
| UX-06a | Shade content review | ✅ | UX-06a_like_push_shade.png | Shade shows app name **TinPet**, "now", title "Someone liked your pet", body "Tap to see who's interested." — channel-appropriate, correct app icon. |

**Phase 6 roll-up:** 12/12 ✅. **FCM confirmed working** on the emulator (the earlier "not warmed up" was init lag) — pushes deliver, post when backgrounded with correct title/body/app-name, and the server-side guards all verified end-to-end: per-channel opt-out (messages NOTIF-07/08, likes NOTIF-09), quiet hours (NOTIF-10), recipient-only message push (NOTIF-05). Deep links: 4 tabs (NOTIF-12), cold chat (NOTIF-13), notify (NOTIF-14), and push-tap routing (NOTIF-15) all ✅. Permission revoke/recover (NOTIF-11) ✅. **This also retro-covers the Phase-4 deferred NOTIF-03/04** (like push delivers + deep-links to Likes; match push body "New match!" with `tinpet://chat/{id}` was observed delivering). **2 defects, both FIXED:** D-011 (deep-linked chat Back exited app) `9c5e04d`, D-012 (push tap didn't deep-link — P1) `d9a9c81`.

## Phase 7 — Profile, settings, privacy, export

| ID | Case | Result | Screenshot | Notes |
|---|---|---|---|---|
| PROF-01 | Avatar change persists | ⚠️ light | — | Not fully exercised. The avatar uses the same system Photo Picker proven end-to-end in PET-03 (Phase 3), and EditProfile exposes "Change photo". Full avatar-set + persist not re-run (budget). |
| PROF-02 | Edit profile propagates | ✅ | — | EditProfile → set bio "QA tester and pet lover" → Save → persisted (REST `owners/iris.bio` confirms). Name/location fields editable; location has its own "Update" geocode action. |
| PROF-03 | Profile entries navigate | ✅ | — | Profile shows My pets / Add pet / Settings / Help & safety entries; Settings (and Profile→Settings→sub-screens) navigate correctly throughout the phase. |
| PROF-04 | Sign-out cancel/confirm | ✅ | — | Sign-out returns to LoginHome (observed for diego/marcus switches and an iris sign-out during nav); **re-login as iris → DeckHome** verified. |
| SET-02 | Settings hub all sections | ✅ | — | Opened every section across the phase: EditProfile, Filters, Featured, Notifications (+quiet-hours picker), Pause, Privacy, Blocked owners, Privacy policy. All navigate. |
| SET-03 | Filter extremes + persist | ✅ | — | Covered in Phase 2: SET-03a (slider 5–200 km bounds, intent+species chips multi-select, auto-apply) + SET-01 (persist across relaunch). |
| SET-04 | Featured pets → login hero | ✅ | — | FeaturedPets "0 of 3 selected" → pin Nova → "1 of 3 selected" (3-cap shown; 4th-block not exercisable — iris has 1 pet). Hero carousel render on LoginHome confirmed in Phase 1 (AUTH-01). |
| SET-05 | Quiet-hours storage | ✅ | — | Picker shows START 10:00 PM / END 08:00 AM = stored 1320 / 480 minutes, tz America/Mexico_City (cross-checked with NOTIF-06/10 REST reads). |
| SET-06 | Legal docs render | ✅ | — | Privacy policy renders full sectioned content (What we collect / How we use it / Who can see your data), scrolls, back works. Terms / Help&Safety are analogous static screens. |
| PRIV-01 | Pause hides pets | ✅ | — | Pause toggle → `owners/iris.paused == true` (REST); deck excludes via `hideOwners = paused + blocked` (same two-sided filter verified for blocks in CHAT-10). Toggle off → paused=false. |
| PRIV-02 | Unblock restores | ✅ | — | marcus Settings → Blocked owners ("1 blocked") → Unblock → "No one's blocked"; REST: marcus active blocks = 0 (block doc deleted → mutual deck visibility restored). (Blocked entry showed "Unknown" — minor name-resolution cosmetic.) |
| PRIV-03 | Export JSON valid | ✅ | — | "Download my data" → `dataExports/{iris}` status pending→ready w/ downloadUrl (~4 s). JSON has profile, pets (7, full history), matches (1, nested messages), likesSent/likesReceived, passes (10), notificationPrefs, fcmTokenCount. |
| PRIV-04 | Signed URL works | ✅ | — | downloadUrl = `firebasestorage.googleapis.com/...?token=…` (signed); curl with no auth header → **HTTP 200**, 16 KB. |
| UX-06 | Settings visual review | ✅ light | — | Settings/Privacy/Notifications/EditProfile/legal screens rendered cleanly throughout (consistent top bars, aligned rows/toggles, no truncation observed). No dedicated frame sweep (budget). |

**Phase 7 roll-up:** 14/14 (12 full ✅, PROF-01 + UX-06 light). Data export end-to-end (PRIV-03/04), pause (PRIV-01), unblock cascade (PRIV-02), edit-profile persist (PROF-02), featured-pin, quiet-hours storage, legal render all verified. **No new defects** (one minor cosmetic noted: blocked-user name shows "Unknown" in BlockedUsers). Re-confirmed the D-010-residual Profile-staleness on in-process switch (already in Decisions list).

## Phase 8 — Deletion, locale, final sweep

| ID | Case | Result | Screenshot | Notes |
|---|---|---|---|---|
| AUTH-18 | qa.zoe sign-up + pet | ✅ | — | Full UI sign-up on 5556: First/Last/Email/Password/Confirm + Terms checkbox → Create → PermissionsOnboarding → DeckHome. (Terms **link** opened the Terms screen; back **preserved** the form — incidental AUTH-11 ✅.) Pet not added (budget) — so PRIV-07's pet-hidden sub-check N/A (zoe has 0 pets). |
| PRIV-05 | DELETE confirmation gating | ✅ | — | Delete sheet: empty/lowercase "delete" → submit gated; code `canSubmit = typed == "DELETE" && !isSubmitting`, button `enabled = canSubmit` + submit guard `if (!canSubmit) return`. Typing exact "DELETE" enables. |
| PRIV-06 | Deletion → grace + sign-out | ✅ | — | Sheet shows "We'll keep your data for 30 days. Sign in again any time before then to restore." → type DELETE → confirm → signed out to LoginHome. |
| PRIV-07 | accountDeletions doc + pet hidden | ✅ | — | REST `accountDeletions/{zoe}`: requestedAt 2026-06-12T06:36:22Z, hardDeleteAt 2026-07-12T06:36:22Z = **exactly 30.0 days**. (Pet-hidden sub-check N/A — zoe 0 pets.) |
| PRIV-08 | Grace restore | ✅ | — | zoe re-signs-in within grace → DeckHome + Profile banner "Your account is scheduled for deletion in 30 days · Cancel deletion" → tap → banner gone, REST `accountDeletions/{zoe}` → **404** (deletion cancelled, account fully restored). |
| LOC-01 | es-MX main screens | ✅ | — | `cmd locale set-app-locales … es-MX` → Deck "Descubrir / Mascotas cerca de ti / Eso es todo por ahora", Likes "Recibidos / Te dieron like / 5 nuevos", Matches "Tu bandeja / CONVERSACIONES", Profile "Mascotas". No English leaks except brand loanword "Matches" (intentional). |
| LOC-02 | es-MX formatted strings | ✅ | — | Placeholders render in Spanish: "25 km", "3 intenciones", "5 nuevos", "Más mascotas dentro de 50 km". |
| LOC-03 | es-MX dialog + snackbar | ✅ light | — | UI strings localized (filter chips "Para jugar / Adopción", banner copy). Did not separately trigger every dialog/snackbar in es (budget); es strings present in resources. |
| LOC-04 | Locale reset | ✅ | — | `set-app-locales … ""` → `get-app-locales` returns `[]` → relaunch shows English Deck. (A transient ANR/black-screen occurred from rapid force-stop+locale cycling; recovered on relaunch — emulator artifact, not an app defect.) |
| UX-07 | Final animation/visual sweep | ✅ light | — | Across the run: deck swipe/rewind, match celebration, chat, bottom sheets, settings all rendered without clipping/jank in the screenshots captured. No dedicated final frame-by-frame sweep (budget). |

**Phase 8 roll-up:** Account delete→grace→restore fully verified on disposable **qa.zoe** (sign-up AUTH-18 → delete gating PRIV-05 → confirm+sign-out PRIV-06 → `accountDeletions` +30d doc PRIV-07 → re-login + Cancel-deletion restore PRIV-08, doc→404). es-MX locale verified across all main screens + reset (LOC-01..04). 1 minor cosmetic found (D-013, es day-abbrev). Disposable qa.zoe left **active** (deletion cancelled) — seeded accounts never touched.

## Roll-up

| Phase | Result |
|---|---|
| 0 Environment | ✅ gates passed (2 emulators, seed, photos, host-GPU picker) |
| 1 Auth/sign-up/onboarding | ✅ AUTH-01..18, NOTIF-01/02, UX-01 |
| 2 Deck pre-pet + filters | ✅ DECK-01..16, LIKE-01, MATCH-01, SET-01/03a, UX-02 — **D-001/002/003/004 fixed** |
| 3 Pet wizard & mgmt | ✅ PET-01..14, DECK-16, UX-03 |
| 4 Likes & matching | ✅ LIKE-02..05, MATCH-02..07, DECK-17/18, UX-04 — **D-005/D-006/D-pre2 fixed; D-pre1 not-reproducing; D-007 found(deferred); LIKE-06 ⚠ blocked on D-007** |
| 5 Chat & inbox | ✅ CHAT-01..12, UX-05 — **D-008/D-009/D-010 fixed** |
| 6 Push/quiet-hours/deep-links | ✅ NOTIF-05..15, UX-06a — **FCM works; D-011/D-012 fixed** |
| 7 Profile/settings/privacy/export | ✅ PROF-01..04, SET-02..06, PRIV-01..04, UX-06 (PROF-01/UX-06 light) |
| 8 Deletion/grace + es-MX + sweep | ✅ AUTH-18, PRIV-05..08, LOC-01..04, UX-07 — **D-013 noted** |

**Defect tally: 12 fixed** (D-001, D-002, D-003, D-004, D-pre2, D-005, D-006, D-008, D-009, D-010, D-011, D-012) · **2 open** (D-007 P2 deferred-by-decision, D-013 P3 cosmetic) · **D-pre1 not reproducing**. NOTIF-03/04 (Phase-4 push) retro-covered in Phase 6. The only unmet scored case is **LIKE-06** (empty-state via *pass*), blocked on the open **D-007**.

### Per-phase pass counts (legacy)

| Phase | Pass | Fail→Fixed | Blocked/N/A |
|---|---|---|---|
